# This file is always sourced by zsh.
# Can be useful for non-login, non-interactive shells

# I'd generally prefer to keep behavior and configs between bash & zsh
# similar, so this probably won't get much use? Anyways, for a bash option
# for non-login, non-interactive, see $BASH_ENV.
